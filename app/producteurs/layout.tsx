import type { ReactNode } from "react";

export const metadata = {
  title: "Espace producteurs | GreenCart",
  description: "Tableau de bord, produits et recommandations IA pour les producteurs GreenCart."
};

export default function ProducteursLayout({ children }: { children: ReactNode }) {
  return children;
}
