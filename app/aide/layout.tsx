import type { ReactNode } from "react";

export const metadata = {
  title: "Aide / FAQ | GreenCart",
  description: "Questions fréquentes et aide à l'usage de la plateforme GreenCart."
};

export default function AideLayout({ children }: { children: ReactNode }) {
  return children;
}
